apiVersion: v1
kind: Service
metadata:
  name: {{ include "ollama-moderation-gateway.fullname" . }}
  labels:
    {{- include "ollama-moderation-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ollama-moderation-gateway.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
